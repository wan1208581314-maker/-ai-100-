请根据我提供的 PRD.md 和 UI 参考图，帮我实现一个移动端网页应用《今天吃什么》。

要求：
1. 使用 React + Vite + Tailwind CSS。
2. 页面移动端优先，适配手机浏览器。
3. 底部导航只保留：首页、卡片仓库。
4. 首页实现食物卡片轮播和灶台旋钮。
5. 卡片仓库实现分类筛选和卡片网格。
6. 新增卡片页面从卡片仓库进入，不作为底部导航。
7. 新增卡片支持上传图片、填写菜名、选择分类、添加标签，并生成 3:4 卡片预览。
8. 所有食物卡片必须保持 3:4 比例，不能拉伸，不能压缩。
9. 请先实现静态 UI，不要先做复杂功能。
10. UI 必须尽量还原参考图。