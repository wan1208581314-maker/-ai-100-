# 像素桌宠启动器

一个浅色可爱像素风桌宠启动器，用来集中管理、切换和启动 `D:\自媒体\claude\第四期` 里的桌宠。

## 当前 MVP

- 自动发现 Luya、Piko、Mimo
- 优先启动已打包的 `dist\...\*.exe`
- 找不到 exe 时回退启动 `.cmd`
- 保存最近选择和最近启动时间
- 像素风主界面：宠物库、CRT 预览屏、详情面板、底部操作栏

## 启动

```bash
npm install
npm start
```
